{
	"name": "vk-link-target-controller",
	"version": "1.3.1",
	"engines": {
		"node": ">=20"
	},
	"description": "Allow you to link a post title from the recent posts list to another page (internal or external link) rather than link to the actual post page",
	"scripts": {
		"update": "ncu -u -x '@wordpress/env' && rm -rf node_modules package-lock.json && npm install",
		"setup:agents": "node _dev/install-agent-skills.mjs",
		"postinstall": "npm run setup:agents",
		"wp-env": "wp-env",
		"build": "wp-scripts build --webpack-src-dir=src --output-path=build && npm run jsmin",
		"build:block": "wp-scripts build --webpack-src-dir=src --output-path=build",
		"start": "wp-scripts start --webpack-src-dir=src --output-path=build",
		"jsmin": "terser js/script.js -o js/script.min.js -c -m",
		"test": "node --check js/script.js && node --check js/admin-link-dialog.js",
		"dist": "composer install --optimize-autoloader --prefer-dist --no-dev && npm run build && bash bin/dist.sh",
		"phpunit": "composer install && wp-env run tests-cli --env-cwd='wp-content/plugins/vk-link-target-controller' vendor/bin/phpunit -c .phpunit.xml --verbose",
		"test:e2e": "npm run build:block && playwright test"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/vektor-inc/vk-link-target-controller.git"
	},
	"author": "Vektor, Inc.",
	"license": "GPL-2.0-only",
	"bugs": {
		"url": "https://github.com/vektor-inc/vk-link-target-controller/issues"
	},
	"homepage": "https://wordpress.org/plugins/vk-link-target-controller/",
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@wordpress/e2e-test-utils-playwright": "^1.53.0",
		"@wordpress/env": "10.38.0",
		"@wordpress/scripts": "^34.0.0",
		"npm-check-updates": "^19.3.1"
	},
	"dependencies": {
		"@babel/runtime": "^7.29.2"
	},
	"overrides": {
		"serialize-javascript": "^7.0.5",
		"markdownlint-cli": {
			"minimatch": "3.1.5"
		},
		"adm-zip": "^0.6.0"
	}
}
